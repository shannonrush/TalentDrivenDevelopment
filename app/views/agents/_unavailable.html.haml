="#{@agent.first_name} is unavailable for new clients at this time. "
-if current_user.pending_notification_for?(@agent)
  ="You will be notified when #{@agent.first_name} becomes available."
-else
  =link_to " - receive notification when #{@agent.first_name} becomes available", notifications_path(agent_id:@agent.id), :method => :post 
