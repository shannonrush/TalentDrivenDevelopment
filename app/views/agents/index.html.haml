%div#search
  =form_tag agents_path, :method => :get do
    =text_field_tag :search, params[:search], :class => 'search'
    =image_submit_tag("search_button.png",:class=>'search_button')

-if @agents.empty?
  %p
    No agents found, please try different keywords
-else
  =render @agents
